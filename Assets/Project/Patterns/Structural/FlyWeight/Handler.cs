using UnityEngine;

namespace Patterns.Structural.FlyweightPattern
{
    public class Handler : MonoBehaviour
    {
        //Эдакий гибрид пула (ага, непонятно только, в каком месте) и фабрики (ага, только пул тоже может быть с фабрикой),
        //позволяющий использовать разделяемые объекты сразу в нескольких контекстах (многоцелевое использование)


        private void Start()
        {
            
        }
        


        private void Update()
        {
            
        }
    }
}